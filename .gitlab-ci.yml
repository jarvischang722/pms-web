# defined stages
stages:
  - build
  - test
  
cache:
  paths:
    - node_modules/
    
    

# defined build
job1:
  stage: build
  tags:
    - bac-prod
  script:
    - whoami
    
# defined test
job2:
  stage: test
  script:
    - /usr/bin/git -v
    - which npm
    - npm -v

    