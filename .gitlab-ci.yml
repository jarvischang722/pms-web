# defined stages
stages:
  - build
  - test
  
cache:
  paths:
    - node_modules/
    

    
    
# 安装依赖
install_deps:
  stage: install_deps
  tags:
    - bac-testing
  only:
    - develop
    - master
  script:
    - whoami
    - /root/.nvm/versions/node/v8.8.0/bin/npm install
    
# defined job
job1:
  stage: test
  tags:
    - bac-testing
  script:
    - echo "I am job1"
    - echo "I am in test stage"
# defined job
job2:
  stage: build
  tags:
    - bac-prod
  script:
    - echo "I am job2"
    - pwd