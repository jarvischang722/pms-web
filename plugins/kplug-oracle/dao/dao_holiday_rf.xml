<?xml version="1.0" encoding="UTF-8"?>
<root>

    <!-- 當年度總日期天數 -->
    <dao name="QRY_HOLIDAY_RF_COUNT">
        <statement><![CDATA[
            select COUNT(*) as dateCount
            from holiday_rf
            where athena_id = trim(:athena_id) and trim(hotel_cod) = trim(:hotel_cod)
            and batch_dat BETWEEN TO_DATE( ? , 'YYYY/MM/DD') AND TO_DATE( ? , 'YYYY/MM/DD')
        ]]></statement>

        <parameter type="string" kind="3">athena_id</parameter>
        <parameter type="string" kind="3">hotel_cod</parameter>
        <parameter type="string" kind="1">startDate</parameter>
        <parameter type="string" kind="1">endDate</parameter>
    </dao>


</root>